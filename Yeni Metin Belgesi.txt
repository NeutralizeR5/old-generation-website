@echo off
set /p EMAIL="trneutralizer@gmail.com: "
set /p USERNAME="NeutralizeR5: "
set /p REPO="https://github.com/NeutralizeR5/old-generation-website.git: "

echo Git kullanıcı bilgileri ayarlanıyor...
git config --global user.email "%EMAIL%"
git config --global user.name "%USERNAME%"

echo Git deposu başlatılıyor...
git init

echo Dosyalar ekleniyor...
git add .

echo Commit oluşturuluyor...
git commit -m "İlk gönderim"

echo Branch adı 'main' olarak ayarlanıyor...
git branch -M main

echo Uzak bağlantı ekleniyor...
git remote add origin %REPO%

echo GitHub'a gönderiliyor...
git push -u origin main

pause
